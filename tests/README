The `interactive' test is meant to be 'sourced' by the Scheme interpreter, as it
loads the `schedl' file, and interactively tests it.

The `unit*' files are targets of the `Makefile'. They are meant to test
`schedl'.
